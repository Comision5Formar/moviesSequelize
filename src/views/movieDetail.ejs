<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h1>
        <%= pelicula.title %>
    </h1>
    <ul>
        <li>Rating: <%= pelicula.rating %>
        </li>
        <li>Premios: <%= pelicula.awards %>
        </li>
        <li>Fecha de estreno: <%= fecha %>
        </li>
        <% if (pelicula.genero) { %>
            <li>Genero : <a href="/generos/detalle/<%= pelicula.genero.id %> ">
                    <%= pelicula.genero.name %>
                </a> </li>
            <% } %>
    </ul>
    <h4>Actores</h4>

    <ul>
        <% pelicula.actores.forEach(actor=> { %>
            <li>
                <a href="/actores/detalle/<%= actor.id %> ">
                    <%= actor.first_name %>
                        <%= actor.last_name %>
                </a>

            </li>
            <% }) %>
    </ul>
    <a href="/peliculas/editar/<%= pelicula.id %> ">Editar</a>

    <form action="/peliculas/eliminar/<%= pelicula.id %>?_method=DELETE " method="POST">
        <button type="submit">Eliminar</button>
    </form>

</body>

</html>